# Agent instructions for this project

## Testing
Run `pytest tests/` or `python -m pytest tests/`

## Linting
Format with `black .` before committing.

## Data
Always regenerate data before testing: `python -m egrid_learning.data.generator`
